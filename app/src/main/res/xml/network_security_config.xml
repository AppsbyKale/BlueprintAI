<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Enable cleartext HTTP for local LAN servers (LM Studio / Ollama on local IP addresses) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
